# Prime Heroes Wiki — Agent Notes

- This is a pre-launch tracker: publish only announcement-stage facts from official/press sources. Never invent hero names, ability values, or dates.
- Hero/strategy pages are added only after Early Access opens and content is verifiable in-game.
- Next content trigger: exact early access date announcement, then official hero reveals.
- Verify with `npm run check`, `npm run lint`, `npm run build` (all must pass before push).
- IndexNow key file lives in `public/`.
